pipeline{
    agent{
         docker {image 'alpine:latest'}
    }
    stages{
        stage('Deploy to Github Pages'){
            steps{
                sh 'apk update'
                sh 'apk add python3'
                sh 'project=$(pwd)'
                sh 'echo $project'
            }
        }
    }
}