pipeline{
    agent{
         docker {image 'alpine:latest'}
    }
    stages{
        stage('Deploy to Github Pages'){
            steps{
                sh 'sudo apk add python3'
                sh 'project=$(pwd)'
                sh 'echo $project'
            }
        }
    }
}