pipeline{
    agent{
         docker {image 'alpine:latest'}
    }
    stages{
        stage('Deploy to Github Pages'){
            steps{
                sh 'apk add python3'
                sh 'uname -a'
                sh 'project=$(pwd)'
                sh 'echo $project'
            }
        }
    }
}