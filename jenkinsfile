pipeline{
    agent{
         docker {image 'shinn5112/documentation:latest'}
    }
    stages{
        stage('Deploy to Github Pages'){
            steps{
                sh '''
                    sphinx-build --version
                    doxygen --version
                    build_dir=$(pwd)
                    echo "$build_dir"
                '''
                
            }
        }
    }
}