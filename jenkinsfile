pipeline{
    agent{
         docker {image: 'alpine:latest'}
    }
    stages{
        stage('Deploy to Github Pages'){
            steps{
                sh 'project=$(pwd)'
                sh 'echo $project'
            }
        }
    }
}